# In this file I have all the general configurations and data

# environment/computational parameters
device: cuda
num_workers: 4

# dataset parameters
data_root: /Users/annaboser/Documents/GitHub/lst-super-res/data

# training hyperparameters
image_size: [224, 224]
num_epochs: 200
batch_size: 128
learning_rate: 0.001
weight_decay: 0.001